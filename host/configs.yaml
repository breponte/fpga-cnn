# Socket connection
host: "127.0.0.1"
port: 5000

# Dataset information
total_images: 10000
batch_size: 1      # both size of CNN and data transmission
batch_count: 5      # number of CIFAR-10 training batch files
num_channels: 3     # RGB images
num_classes: 10     # number of classes in CIFAR-10 dataset
image_width: 32     # square images
